<div class="container">
  <div class="page-head d-flex justify-content-between align-items-start flex-wrap gap-2 mb-4">
    <div>
      <h1 class="mb-1">{{#if showArchived}}Archived Projects{{else}}Projects{{/if}}</h1>
      <p class="text-muted mb-0">{{#if showArchived}}Projects hidden from the dashboard. Restore or permanently delete them.{{else}}Manage your security assessment projects{{/if}}</p>
      <div class="rule"></div>
    </div>
    <div class="d-flex gap-2">
      {{#if showArchived}}
        <a href="/admin/projects" class="btn btn-outline-secondary">
          <i class="bi bi-arrow-left me-1"></i>{{t 'ui.activeProjects'}}
        </a>
      {{else}}
        {{#if archivedCount}}
          <a href="/admin/projects?archived=1" class="btn btn-outline-secondary">
            <i class="bi bi-archive me-1"></i>{{t 'ui.archived2'}} <span class="badge bg-secondary ms-1">{{archivedCount}}</span>
          </a>
        {{/if}}
        <a href="/admin/projects/new" class="btn btn-primary">
          <i class="bi bi-plus-lg me-1"></i>{{t 'ui.newProject'}}
        </a>
      {{/if}}
    </div>
  </div>

  <div class="card">
    <div class="card-body p-0">
      {{#if projects.length}}
        <div class="table-responsive">
          <table class="table table-hover mb-0">
            <thead>
              <tr>
                <th>{{t 'ui.projectName'}}</th>
                <th>{{t 'ui.classification'}}</th>
                <th>{{t 'ui.hosting'}}</th>
                <th>{{t 'ui.owner'}}</th>
                <th>{{t 'ui.status'}}</th>
                <th>{{t 'ui.created'}}</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              {{#each projects}}
                <tr>
                  <td>
                    <a href="/admin/projects/{{this.id}}" class="fw-semibold text-decoration-none">{{this.name}}</a>
                    {{#if this.has_pii}}<span class="badge bg-danger ms-1" title="{{t 'ui.containsPii'}}">PII</span>{{/if}}
                  </td>
                  <td><span class="badge bg-info">{{this.data_classification}}</span></td>
                  <td class="text-muted">{{this.hosting_type}}</td>
                  <td class="text-muted">{{this.project_owner_name}}</td>
                  <td>{{{statusBadge this.status}}}</td>
                  <td class="text-muted small">{{formatDate this.created_at}}</td>
                  <td class="text-end text-nowrap">
                    <a href="/admin/projects/{{this.id}}" class="btn btn-sm btn-outline-primary" title="{{t 'ui.viewProject2'}}">
                      <i class="bi bi-eye"></i>
                    </a>
                    {{#if ../showArchived}}
                      <form action="/admin/projects/{{this.id}}/unarchive" method="POST" class="d-inline ms-1">
                        <button type="submit" class="btn btn-sm btn-outline-secondary" title="{{t 'ui.restoreProject'}}">
                          <i class="bi bi-arrow-counterclockwise"></i>
                        </button>
                      </form>
                    {{else}}
                      <form action="/admin/projects/{{this.id}}/archive" method="POST" class="d-inline ms-1">
                        <button type="submit" class="btn btn-sm btn-outline-secondary" title="{{t 'ui.archiveProject'}}">
                          <i class="bi bi-archive"></i>
                        </button>
                      </form>
                    {{/if}}
                    <a href="/admin/projects/{{this.id}}#delete" class="btn btn-sm btn-outline-danger ms-1" title="{{t 'ui.deleteProject2'}}">
                      <i class="bi bi-trash"></i>
                    </a>
                  </td>
                </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      {{else}}
        <div class="p-5 text-center text-muted">
          <i class="bi bi-folder-plus" style="font-size:3rem"></i>
          <h4 class="mt-3">{{t 'ui.noProjectsYet2'}}</h4>
          <p>{{t 'ui.createYourFirstProjectTo'}}</p>
          <a href="/admin/projects/new" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i>{{t 'ui.createProject'}}</a>
        </div>
      {{/if}}
    </div>
  </div>
</div>
