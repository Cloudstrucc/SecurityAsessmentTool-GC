<div class="container" style="max-width:900px">
  <div class="d-flex align-items-center mb-4">
    <a href="/respond/{{inviteCode}}" class="btn btn-outline-primary me-3"><i class="bi bi-arrow-left"></i></a>
    <div>
      <h1 class="mb-0">{{t 'checklist.h1.iato_remediation_checklist'}}</h1>
      <p class="text-muted mb-0">{{assessment.project_name}}</p>
    </div>
  </div>

  <div class="alert alert-warning">
    <i class="bi bi-exclamation-triangle me-2"></i>
    Your project has received an <strong>{{t 'checklist.strong.interim_authority_to_operate_iato'}}</strong>. The items below must be addressed
    by their deadlines before a full ATO can be granted.
  </div>

  {{#if items.length}}
    <div class="card">
      <div class="card-body p-0">
        <div class="table-responsive">
          <table class="table table-hover mb-0">
            <thead>
              <tr>
                <th style="width:5%">#</th>
                <th style="width:35%">{{t 'checklist.th.description'}}</th>
                <th>{{t 'checklist.th.control'}}</th>
                <th>{{t 'checklist.th.deadline'}}</th>
                <th>{{t 'checklist.th.assigned_to'}}</th>
                <th>{{t 'checklist.th.status'}}</th>
              </tr>
            </thead>
            <tbody>
              {{#each items}}
                <tr class="{{#eq this.status 'completed'}}table-success{{/eq}} {{#eq this.status 'overdue'}}table-danger{{/eq}}">
                  <td>{{inc @index}}</td>
                  <td>{{this.description}}</td>
                  <td>{{#if this.control_id}}<code>{{this.control_id}}</code>{{else}}—{{/if}}</td>
                  <td>
                    {{formatDate this.deadline}}
                  </td>
                  <td>{{this.assigned_to}}</td>
                  <td>{{{statusBadge this.status}}}</td>
                </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <div class="mt-3">
      <div class="d-flex justify-content-between align-items-center">
        <div>
          <span class="fw-semibold">{{t 'checklist.span.progress'}}</span>
          {{#each items}}{{#eq this.status "completed"}}{{/eq}}{{/each}}
        </div>
      </div>
    </div>
  {{else}}
    <div class="card">
      <div class="card-body text-center text-muted py-5">
        <i class="bi bi-list-check" style="font-size:3rem"></i>
        <p class="mt-3 mb-0">{{t 'checklist.p.no_checklist_items_have_been'}}</p>
      </div>
    </div>
  {{/if}}

  <div class="text-center mt-4 mb-5">
    <p class="text-muted">{{t 'checklist.p.contact_your_security_assessor_if'}}</p>
    <a href="/respond/{{inviteCode}}" class="btn btn-outline-primary">
      <i class="bi bi-arrow-left me-1"></i>Back to Assessment
    </a>
  </div>
</div>
